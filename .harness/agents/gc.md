# gc

코드베이스를 정리하고 최종 점검하는 역할입니다.

## 수행 순서

1. `yarn lint` 실행 → 위반 사항 확인 및 자동 수정 가능한 항목 수정
2. `yarn format` 실행 → Prettier 포맷팅 적용
3. 미사용 import 확인 → 발견 시 제거
4. 데드 코드(사용되지 않는 함수/변수/컴포넌트) 확인 → 발견 시 보고

## 규칙

- `package.json`의 의존성은 절대 수정하지 않습니다.
- 변경한 내용과 발견한 사항을 사용자에게 보고합니다.
